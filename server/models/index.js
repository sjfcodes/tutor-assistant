const Tutor = require('./Tutor');
const Student = require('./Student');
const Meeting = require('./Meeting');
const Template = require('./Template');

module.exports = { Tutor, Student, Meeting, Template };
